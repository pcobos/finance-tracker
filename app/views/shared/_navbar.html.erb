<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to "Finance Tracker", root_path, class: "navbar-brand" %>
    <% if user_signed_in? %>
      <button class="navbar-toggler" type="button"    aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
  
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          Profile
        </a>
          <div class="dropdown-menu drop-box" aria-labelledby="navbarDropdown">
            <%= link_to edit_user_registration_path, class:'nav-link dropdown-item' do %>
              <%= fa_icon 'edit' %> Edit profile
            <% end %>
          </div>
        </li>
        <li class="nav-item <% 'active' if request.path == my_portfolio_path  %>">
            <%= link_to 'My Portfolio', my_portfolio_path, class:'nav-link' %>
        </li>
        <li class="nav-item <% 'active' if request.path == friends_path  %>">
            <%= link_to 'Friends', friends_path, class:'nav-link' %>
        </li>
        </ul>
      </div>
    <% end %>
    
      <div class="ml-auto row">
        <% if user_signed_in? %>
        <ul class="navbar-nav">
          <li class="nav-item user-name">
            <%= fa_icon 'user', class: "mr-1" %> 
            <% unless current_user.first_name.nil? || current_user.last_name.nil? %>
              <%= current_user.full_name %>
            <% end %>
          </li>
        </ul>
          <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger" %>
        <% else %>
          <%= link_to "Log in", new_user_session_path, class: "btn btn-outline-primary mr-sm-2" %>
          <%= link_to "Sign up", new_user_registration_path, class: "btn btn-outline-primary" %>
        <% end %>
      </div>
  </div>
</nav>
